---
page-id: 3401221248
page-title: CRs Register
page-version: 
page-url: https://example.atlassian.net/wiki/spaces/Restitch/pages/3401221248/CRs+Register
parent-page-id: 3401220005
pulled-on: 12 September 2026
---

# CRs Register

Legend

| Indicator | Meaning |
|---|---|
| ✅ | There is a financial impact |
| ✖️ | No financial impact |
| :plus: | A credit for T&T |
| OPEN | CR is ready for functional and technical review, excluding the financial impact/value |
| WIP | CR is still undergoing further assessment and elaboration |
| approved | CR has been confirmed and approved by T&T |

Vendor ref below is derived from the rendered row number on the source page as CR2-<#>, not held in the page itself.

| # | Vendor ref | Domain | Requirement | Description | Impact | Status | Financial Impact |
|---|---|---|---|---|---|---|---|
| 1 | CR2-1 | SubscriberTrimming | Support modify Bolt scenario | When the Millhouse Supply service is modified with a new Bolt, this may change the Delivery service lots Lot pools and/or till. Hence the SubscriberTrimming should support a modify scenario to change the Bolt |  | approved | ✅ |
| 2 | CR2-2 | SubscriberTrimming | Support modify lot and feeder-routes lots | Modify lot (add or remove lots to the SubscriberTrimming). Modify existing lots is not supported, Shopfront will have to send a modify order to delete the old lot and add a new one wit the desired gauge size. It is also required to support feeder-routes lots.Requirements Specifications:<br>Only one lotA_if (for fitting) and<br>There could be n _fr (feeder route lotA) gauge<br>The till will be the one associated with the lotA_if lookup entry<br>If the lotAllocation is shared, then we can't have feeder route gauge<br>Feeder route lots will be configured in the Tallyboard "feeder-route", not impacting the Dispenser configuration | Workflow steps:<br>release old lot, if not reused<br>allocate new ones with the new size<br>update dispenser configuration (applicable only in PlainAuth), if the added/deleted lots are _if, _na, _pd <br>update tallyboard configuration, if the added/deleted lots are _if, _fr<br>bounce the Winder feed (clear feed)GDA641 payload <br>The new full lots array has to be passed.<br>Existing lots (which should be kept with no change) have to include the reservationId.<br>New added lots will not have the allocated reservationId.<br>Missing lots will be deleted/released. | approved | ✅ |
| 3 | CR2-3 | SubscriberTrimming | Support for portability of Lot lots | Shopfront can request a specific Lot lot in the order. The lot should be in the quarantine period in the LotRegistry, otherwise LotRegistry will reject the reassignment request. The allocation Id will be used to identify the requested lot.<br>Applicable for lotA_if, lotA_fr, lotB_na, lotB_pd<br>Can be requested in connect and in modify scenarios<br>Shopfront will have to pass the allocationId in the order | Workflow steps:<br>The SubscriberTrimming lots service characteristic will be enriched with the allocationId.<br>If Shopfront passed the allocationId in the add/modify orders, the SubscriberDispatch workflow will realize that it is a “portability of Lot lots“.<br>A new LotRegistry API to be used taking the allocationId | approved | ✅ |
| 4 | CR2-4 | SubscriberTrimming | Rainy-day Handling for lotB lots mismatch when found to be on different super-lot | Ipv6 lot for both na and pd should be on the same superlot . | Workflow steps:If after LotRegistry allocation the logic found that the superlot id is not the same for both the allocated _na and _pd lots<br>Open a fallout with action as retry<br>Someone fix the Lot pools<br>WeftSync sync to Bobbin, or manual update to the Bobbin Lot Pool table<br>Submit a retry<br>Rollback both the the lotB allocations<br>Roll forward allocating the lotB lots again | approved | ✅ |
| 5 | CR2-5 | MillhouseG4Supply | Replace modify orderSla with modify serviceRestorationSla | Millhouse doesn’t support modify orderSla, however modifying serviceRestorationSla is a valid scenario |  | approved | ✖️ |
| 6 | CR2-6 | MillhouseG4Supply | Descope some Amend Scenarios | Descope the following Amend Scenarios (mentioned in HLD, however not required):<br>Speed<br>orderSla <br>Finisher Serial Number<br>Serial Mark |  | approved | ✅ :plus: |
| 7 | CR2-7 | MillhouseG4Supply | Modify Speed order items dependencies | If we have Supply and Deliver order concurrent (different items under the same order - two parallel orders is not supported) then there should be an order items dependency management to ensure that the services are modified in the right sequence<br>In upgrades -> Supply then Delivery (if the new deliver is higher than the old Supply)<br>In downgrade -> Delivery then Supply<br>If a modify Delivery is received to change the Delivery to a higher speed than the Supply, then the order should be rejected<br>Supply 100Mbs, Delivery 60Mb - apply this logic to the downstream speed<br>Modify increase the Delivery to 80Mb -> accepted<br>Modify increase the Delivery to 120Mb -> rejected<br>Modify (in a single service order) Supply to 120, Delivery 160 -> rejected<br>Modify (in a single service order) Supply to 120, Delivery 120 -> change the Supply first then the Delivery<br>Modify (in a single service order) Supply to 40, Delivery 40 -> change the Delivery first then the Supply<br>This logic is applicable to only Supply-Delivery which has a 1:1 relationship, G4 for now.<br>The solution to allow extension in the future to support these scenarios. <br>In this project scope, the validation will be between MillhouseG4Supply and SubscriberTrimming delivery, or between MillhouseG4Supply and CarrierRibbon delivery services. |  | approved | ✅ |
| 8 | CR2-8 | MillhouseG4Supply | Override and retry failed/rejected Millhouse product orders | Allow users to interfere when Millhouse reject orders and override the Millhouse product order payload. This is applicable for ProductOfferingQualification and PorductOrder calls.<br>Millhouse orders rejections and failures will trigger the fallout management module, which will lookup the rejection/failure status and code and derive the desired action. Actions can be either:<br>open a manual fallout allowing the operators to retry with the option to override the Millhouse payload.<br>notify Shopfront to cancel the order and then wait for cancellation | Workflow changes:<br>Move the POQ and its prerequisites steps from the validation phase (pre accepting the order - i.e. before moving the order to inProgress) to the execution phase. Prerequisites steps include TPI selection, Bolt selection, Finisher replace logic.<br>When there is a failure or rejection to the POQ, Appointment or the PO calls, initiate the Fallout Management | approved | ✅ |
| 9 | CR2-9 | MillhouseG4Supply | Finisher Replace Logic Extension | Extend the Finisher Replace logic to handle most of the commonly know scenario. The objective is to optimize the order cost trying to avoid unnecessary appointments whenever possible. |  | WIP | ✅ |
| 10 | CR2-10 | MillhouseG4Supply | Support Service Transfer “Connect Outstanding“ scenario | Scenario:<br>Customer moved to a location with an active service, but they don't know the Cut ID.<br>EC will NOT share the serviceIdToTransfer in the order.<br>T&T Shopfront will verify the user is truely living in that location and is illegible to request Connect Outstanding“<br>The Backroom GDA641 order will use the CAD while interacting with MillPortal APIs | Extend the MillhouseG4Supply.validate and the GDA645 operations to support the following <br>Run ESQ with CAD<br>If no active service -> reject, same like the Service Transfer scenario<br>If one active service is returned (99%) -> happy day, Backroom will use it<br>If more than one active service at that location → reject, indicating that more than one active service is available at that location.<br>Shopfront can query all the active services details (Finisher/Bay, TPI, SRI, Service Supplier Id) by running the GDA645 request directly from outside<br>After consulting with the EC, Shopfront will then place an order specifying the (Finisher/Bay, TPI) to use<br>The Product Order payload will follow the same design as the normal connect order with the “transferType“ as “Connect Outstanding“ | OPEN | ✖️ |
| 11 | CR2-11 | MillhouseG4Supply | Enhance the GDA645 to qualify the appointment requirement in details | GDA645 is going to be used by Shopfront quite heavily during order capturing to assess if an appointment is needed. It is better solution to move this logic into the GDA645 instead of the Shopfront interrogates the siteRestriction native response and then derive the logic accordingly | Workflow changes:<br>GDA645 checkServiceQualification workflow<br>MillhouseG4Supply add and modify workflows to leverage the new GDA645 | WIP | ✅ |
| 12 | CR2-12 | MillhouseG4Supply | Query Appointment API | New API GDA646 GET to allow Shopfront to query MillPortal appointment API to retrieve the available slots for the EC to choose from. |  | WIP | ✅ |
| 13 | CR2-13 | CustomerCounter | Services Loading Mechanism | Replace the “lazy loading” technique in the HLDInstead of using GDA641 to load existing Counter service, the solution will execute a sequence of APIs directly towards Bobbin | GDA921/GDA685 (TBC) allocate the wvi<br>GDA921/GDA685 (TBC) allocate the wsi<br>Use GDA638 to instantiate the Counter service model creating the CustomerCounter, Counter, CNTB, Cbundle<br>GDA921/GDA685 (TBC) Pool Create the pool (Counter CREEL pool)<br>GDA921/GDA685 (TBC) to bulk allocate the assigned reel<br>notes:<br>Assign a new wvi, that will be used for new services<br>Same as in wvi, Assign a new wsi (wsi is required if it is Active/Active or Active/Passive, i.e. redundant mode)<br>GDA641 shouldn't be used in this context bulk allocating the Counters<br>Bobbin will expose these TMF APIs and they all maintain transaction boundary around | approved | ✖️ |
| 14 | CR2-14 | Pool Management | GDA685 resourcePoolManagement | Replace all the customer actions under seciton “2.7.6 Number Management Actions“ in the Bobbin HLD.Expose GDA685 API to Shopfront allowing consumers to manage pools resources.The API should support the following capabilities:<br>Creating Reel pools<br>Allocate and release Reels<br>Creating identifiers pools (WVI, WSI, BundleId, PW)<br>Allocate and release identifiers from their respective pools |  | approved | ✖️ |
| 15 | CR2-15 | Location Management | GDA674 geographicSite | Expose GDA674 API to Shopfront allowing consumers to create (POST) and query (GET) geographicSite objects. The API should support the following capabilities:<br>Creating Depots/Locations with only with the standard TMF attributes<br>Creating Locations with includes the extended attributes<br>Creating Locations with reference to DSA<br>Updating Locations with reference to DSA<br>Query a specific Depot/Location with its Id.<br>Query Depots/Locations, filtering by their attributes |  | WIP | ✅ |
| 16 | CR2-16 | SubscriberTrimming | Support "subscriberProtocol" as a modifiable characteristic | Support changing the subscriberProtocol from PlainAuth to NamedAuth and vice versa.<br>Moving from PlainAuth to NamedAuth will remove the Dispenser configuration, while the reverse will enforce Dispenser configuration<br>Change the Tallyboard configuration <br>Trigger the Winder clear feed<br>No impact on LotRegistry, lots allocations are not impacted |  | approved | ✅ |
| 17 | CR2-17 | SubscriberTrimming | Winder Feed Management changes | The following changes are required to the Bobbin model, federation adaptor, and process flows:<br>Bolt-TAPE-WRG-Winder model update and WeftSync federation<br>TAPE can have direct relationship towards the Winder machine, not through WRG<br>Only one, no redundancy<br>Move the groupId from the WRG to the Bolt class [Blythe to confirm]<br>Remove the resourceId from the WRG<br>Passing Winder machineId into the WinderFeedManagement calls<br>Add the Winder clear feed task to the SubscriberDispatch delete operation | Bobbin Model and WeftSync Federation changes<br>Change the model<br>Change the WeftSync federation adaptorBNG Feed Management<br>Need to pass the machineId in the Winder calls, which represents the Winder machineId that is federated from WeftSync<br>Warp/Bobbin will need to trace the path from the Bolt all the way to the Winder machineId in the inventory<br>In case of redundancy, we pass both machine Ids in one single call <br>To be sent as array of Integer, even if one machine Id<br>No "feed-id" to be sent to the Winder, we will send "username” in addition to the “machineId[]”<br>SubscriberDispatch delete flow will initiate the clear feed API | approved | ✅ |
| 18 | CR2-18 | CustomerCounter | Counter Modifications that triggers overlay CarrierRibbon services migrations | Scenarios:<br>TradeCounter Redundancy Mode Update from Redundant to Non-Redundant and vice versa<br>Changing ENN from strand to bundle ether and vice versa<br>Moving all the strands from one node to another These scenarios mandate migrating the overlay services (CarrierRibbon), which also require outage. Two options here<br>Either, migrate (redeploy) services one by one using Loom API inside Warp modify CustomerCounter order. Or<br>Warp modify CustomerCounter order raise a manual task for OPS to run a background script to migrate (redeploy) the impacted services in bulk |  | WIP | ✅ |
| 19 | CR2-19 | MillhouseG4Supply | Support Bolt tenancy in orders | Bolt services are classified and by tenancy.<br>A MillhouseG4Supply order will specify the tenancy, which will derive the Bolt selection logic. | Workflow changes:<br>MillhouseG4Supply connect order will have tenantId as a mandatory input parameter<br>tenantId "1" indicate the shared tenant<br>Some customers will have pre-identified Bolts, where the order will pass their tenant in the orderUIV Changes<br>Extend the MillhouseG4Supply model to have a new characteristic boltTenantId<br>Update the Bolt selection action to filter based on the tenantId, plus the DSA, then consider the boltPriority and randomly select one of the highest priority | OPEN | ✅ |
